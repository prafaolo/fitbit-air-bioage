/**
 * Placeholder for Task 27 (Profile and Connection pages).
 * Task 26 only needs this module to exist and type-check so `App.tsx` can
 * route to it — replace this entire file with the real connection-status /
 * OAuth-start / sync-trigger UI in Task 27.
 */
export function Connection() {
  return (
    <section>
      <h1>Connection</h1>
      <p className="muted">Coming soon.</p>
    </section>
  );
}
